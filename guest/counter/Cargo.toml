[package]
name = "counter-guest"
version = "0.1.0"
edition = "2021"

[dependencies]
polkavm-derive = "0.31.0"
zhost-protocol = { path = "../../crates/zhost-protocol", default-features = false }
zhost-guest-ui = { path = "../../crates/zhost-guest-ui" }

[profile.release]
opt-level = "s"
lto = true
